export default function StatisticsNotAvailable() {
  return (
    <div className="flex items-center justify-center w-full" style={{ height: "300px" }}>
      Install kubernetes metrics on your cluster to see statistics
    </div>
  );
}